# Reproduce the paper's experiments in pipeline/ (the installable library needs only numpy;
# see pyproject.toml [torch] extra). Pinned to the versions used in the paper.
torch>=2.0
transformers==4.44.2
huggingface_hub==0.24.6
datasets==2.21.0
accelerate==0.34.2
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11
matplotlib>=3.7
sentencepiece
